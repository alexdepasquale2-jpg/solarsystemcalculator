{
  "name": "still-tank",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A case of living weather. You change one thing. You live with the next version.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
